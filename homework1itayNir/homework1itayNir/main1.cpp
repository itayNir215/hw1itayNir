#include "queue.h"
#include <iostream>
/*int main() {
	Queue* q = new Queue;
	initQueue(q, 3);
	enqueue(q, 1);
	enqueue(q, 2);
	enqueue(q, 3);
	std::cout << dequeue(q) << std::endl;
	enqueue(q, 4);
	for (int i = 0; i < 3; i++) {
		std::cout << dequeue(q) << std::endl;
	}
	cleanQueue(q);
	delete q;
}
*/